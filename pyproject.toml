[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mi-paquete"
version = "0.1.0"
description = "Paquete de ejemplo para demostrar CI/CD y construcción"
authors = [{name = "Tu Nombre", email = "tu@correo.example"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
